apiVersion: v1
kind: Service
metadata:
  name: hospital-system-service
spec:
  type: ClusterIP
  selector:
    app: hospital-management-system
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

apiVersion: v1
kind: Service
metadata:
  name: HospitalManagementSystem
spec:
  selector:
    app: HospitalManagementSystem
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
